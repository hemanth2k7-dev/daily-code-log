info:
  name: Testing
  type: http

http:
  method: POST
  url: http://localhost:3000/
  body:
    type: json
    data: |-
      {
        "Hem": "Developer"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
